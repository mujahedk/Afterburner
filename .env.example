DATABASE_URL=postgresql+psycopg://afterburner:afterburner@localhost:5432/afterburner

# Optional: give the worker a fixed ID for easier log tracing (default: worker-<uuid>)
# WORKER_ID=worker-local-1
